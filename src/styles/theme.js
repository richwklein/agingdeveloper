import {createTheme} from "@mui/material/styles";

const theme = createTheme({
  typography: {
  },
  palette: {
    primary: {
      light: "#62727b",
      main: "#37474f",
      dark: "#102027",
      contrastText: "#ffffff",
    },
    secondary: {
      light: "#bef67a",
      main: "#8bc34a",
      dark: "#5a9216",
      contrastText: "#000000",
    },
  },
});

export default theme;
