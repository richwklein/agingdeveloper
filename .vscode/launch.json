{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Development server",
      "command": "npm run dev",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Preview server",
      "command": "npm run preview:build",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
